---
name: post
description: Compose and dispatch today's edition of the plannen-post newspaper.
argument-hint: ""
---

Invoke the `post-compose` skill to compose and dispatch today's edition.

Do not re-implement the workflow in this command — the skill is the source of truth. Locate `skills/post-compose/SKILL.md` inside this plugin (the directory passed via `claude --plugin-dir`, or wherever this plugin is installed) and follow it exactly.
